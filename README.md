# 🏦 AI Business Analyst Bot

AI-бот для автоматизации работы бизнес-аналитика в банке. Ведёт диалог с сотрудниками, собирает бизнес-требования и генерирует документацию для Confluence.

## ✨ Возможности

- 💬 **Умный диалог** — бот задаёт уточняющие вопросы как живой бизнес-аналитик
- 📋 **Сбор требований** — автоматически извлекает цели, scope, KPI, user stories
- 📄 **Генерация документов** — создаёт структурированные BRD в формате Confluence
- 📊 **Диаграммы процессов** — генерирует визуальные диаграммы через Gemini API
- 🔗 **Confluence интеграция** — автоматическая публикация документов

## 🚀 Быстрый старт

### Требования
- Python ≥ 3.10
- Node.js ≥ 18
- Gemini API ключ (бесплатно на [Google AI Studio](https://aistudio.google.com/app/apikey))

### 1. Клонирование и настройка

```bash
git clone https://github.com/your-repo/Forte_AI_Hackathon.git
cd Forte_AI_Hackathon

# Создаём виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или: venv\Scripts\activate  # Windows

# Устанавливаем зависимости
pip install -r backend/requirements.txt
cd frontend && npm install && cd ..
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
# Обязательно
GEMINI_API_KEY=your_gemini_api_key_here

# Опционально (для Confluence)
CONFLUENCE_URL=https://your-domain.atlassian.net/wiki
CONFLUENCE_EMAIL=your-email@example.com
CONFLUENCE_API_TOKEN=your_token
CONFLUENCE_SPACE_KEY=YOUR_SPACE
CONFLUENCE_PARENT_PAGE_ID=123456
```

### 3. Запуск

**Терминал 1 — Backend:**
```bash
source venv/bin/activate
python -m uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
```

**Терминал 2 — Frontend:**
```bash
cd frontend
npm run dev
```

Откройте http://localhost:5173 🎉

## 📁 Структура проекта

```
Forte_AI_Hackathon/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI endpoints
│   │   ├── models.py            # SQLAlchemy models
│   │   ├── schemas.py           # Pydantic schemas
│   │   ├── config.py            # Configuration
│   │   ├── ai/
│   │   │   └── model.py         # AI logic & prompts
│   │   └── integrations/
│   │       └── confluence.py    # Confluence API
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── App.jsx
│   │   ├── pages/
│   │   │   ├── Home.jsx
│   │   │   ├── ChatSession.jsx
│   │   │   └── Sessions.jsx
│   │   ├── api.js
│   │   └── styles.css
│   └── package.json
├── .env.example
├── .gitignore
└── README.md
```

## 🔌 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/chat/message` | Отправить сообщение в чат |
| GET | `/chat/history/{session_id}` | История диалога |
| POST | `/chat/finish` | Завершить и сгенерировать документ |
| POST | `/diagram/generate` | Сгенерировать диаграмму |
| GET | `/sessions` | Список сессий |
| DELETE | `/sessions/{id}` | Удалить сессию |
| GET | `/document/{session_id}` | Получить документ |
| GET | `/health` | Проверка статуса |

## 🛠 Технологии

- **Backend:** FastAPI, SQLAlchemy, Python 3.12
- **Frontend:** React, Vite, React Router
- **AI:** Google Gemini API
- **Database:** SQLite (по умолчанию)
- **Integration:** Confluence REST API

## 📝 Как использовать

1. **Начните сессию** — нажмите "Начать сессию" на главной
2. **Опишите задачу** — напишите что нужно сделать (например: "Нужна система уведомлений о просрочке")
3. **Отвечайте на вопросы** — бот уточнит детали
4. **Завершите** — нажмите "Завершить" для генерации документа
5. **Получите результат** — документ опубликуется в Confluence

## 🔧 Конфигурация

### Gemini API
1. Перейдите на https://aistudio.google.com/app/apikey
2. Создайте API ключ
3. Добавьте в `.env`: `GEMINI_API_KEY=AIza...`

### Confluence
1. Создайте API токен: https://id.atlassian.com/manage-profile/security/api-tokens
2. Заполните переменные в `.env`

## 🐛 Устранение проблем

| Проблема | Решение |
|----------|---------|
| CORS ошибка | Убедитесь что backend запущен на порту 8000 |
| "Сервис недоступен" | Проверьте GEMINI_API_KEY в .env |
| 404 на / | Это нормально, API на /health |
| Gemini quota exceeded | Подождите или используйте другой ключ |

## 📄 Лицензия

MIT License

## 👥 Авторы

Forte AI Hackathon Team
