Правильный способ отображения диаграмм Mermaid

Чтобы диаграмма Mermaid выводилась как настоящая блок-схема, нужно обеспечить правильное форматирование Markdown и корректный синтаксис Mermaid. Вот основные шаги:

Используйте код-блок с языком mermaid. Убедитесь, что текст диаграммы заключён в тройные бектики с указанием языка mermaid. Например:

```mermaid
graph TD;
    A-->B;
    A-->C;
    B-->D;
    C-->D;


Такой формат позволяет Markdown-рендереру (и Confluence) распознать содержимое как диаграмму Mermaid:contentReference[oaicite:0]{index=0}. В вашем случае, заголовок пункта списка `12. Диаграмма процесса (Mermaid)` должен быть отделён от кода диаграммы пустой строкой, либо сам код должен быть оформлен как вложенный блок кода. Иначе Markdown воспринимает строки диаграммы как обычный текст списка. Использование тройных бектиков ```` ```mermaid ... ``` ```` решает эту проблему.



Следите за синтаксисом Mermaid – избегайте недопустимых символов в тексте диаграммы. Частая причина, почему диаграмма не рендерится (а отображается как текст), – ошибка парсинга Mermaid. В вашем примере в тексте узла присутствуют кавычки: F[Просмотр ЛК: Курс "Записан"]. Двойные кавычки внутри метки узла недопустимы без экранирования. Они приводят к сбою парсера, и в результате ваш компонент MermaidBlock вызывает fallback с выводом текста вместо диаграммы. Решение — убрать или экранировать кавычки. Например, можно просто убрать лишние кавычки вокруг слова Записан или заменить их на одинарные кавычки. Правильный вариант:

F[Просмотр ЛК: Курс 'Записан']


Либо экранировать кавычки, обрамляя весь текст узла в дополнительные кавычки. Для flowchart Mermaid поддерживает вложение текста узла в кавычки или фигурные скобки. Например: F["Просмотр ЛК: Курс \"Записан\""]. Обратите внимание, здесь для внутренней цитаты использованы \". Общий совет: если в тексте узла есть спецсимволы (кавычки, знаки %, ?, слова вроде end и т.п.), заключите весь текст узла в кавычки
stackoverflow.com
. Такой подход показан в официальной документации: спецсимволы внутри текста узла требуют обрамления кавычками, чтобы не «сломать» диаграмму.

Разбейте определение диаграммы на несколько строк по логическим блокам. Хотя Mermaid допускает использование точки с запятой ; для разделения команд в одну строку, на практике лучше писать каждое отношение/узел с новой строки. Это повысит читабельность и уменьшит вероятность ошибок. Например, ваш код можно оформить так:

graph TD
  subgraph Сотрудник  
    A[Авторизация] --> B(Просмотр каталога курсов)  
    B --> C{Выбор курса}  
    C -- Курс найден --> D[Запись на курс]  
    D --> E{Уведомление о записи и приглашение}  
    E --> F[Просмотр ЛК: Курс 'Записан']  
    F --> G(Прохождение курса)  
  end

  subgraph HR/Обучающий отдел  
    H[Админ-панель]  
    H -- Добавить/Редактировать --> I(Создание/Редактирование курса)  
    I --> B  
    I --> J(Управление целевой аудиторией)  
    J --> B  
    K{Ручная отметка прохождения}  
    H -- Выгрузка отчетов --> L[Отчетность]  
  end

  subgraph Система  
    M[Проверка доступности курса/мест]  
    N[Автоматическая отметка прохождения]  
    O[Обновление статуса в ЛК]  
    P[Обновление отчётности]  
    Q[Генерация уведомлений]  
  end

  subgraph Руководитель  
    R[Просмотр отчётов]  
  end

  %% Взаимосвязи между субграфами и узлами:
  D --> M  
  M -- Успешно --> Q  
  Q --> E  
  Q --> HR  
  G -- Онлайн --> N  
  G -- Очное/Вебинар --> K  
  K --> O  
  N --> O  
  O --> P  
  O --> F  
  P --> L  
  P --> H  
  L --> R  


Обратите внимание: кавычки вокруг Записан убраны/заменены. Также название субграфа HR/Обучающий отдел лучше оставить без изменений, так как символ / допускается, но если возникнут проблемы, его можно заменить на дефис или пробел. В таком виде код легче проверить и он более понятен. Если всё прописано корректно, вызов mermaid.parse(src) и mermaid.render(...) отработает без ошибок, и вы получите SVG-диаграмму вместо текста.

Убедитесь, что Mermaid подключён и инициализирован правильно во фронтенде. Судя по коду, вы используете mermaid.initialize({ startOnLoad: false, ... }) и самостоятельно вызываете рендер. Это нормально. Альтернативно, можно позволить Mermaid сам обнаруживать блоки: если бы вы оставили диаграмму в HTML как <div class="mermaid">...</div> и вызывали mermaid.contentLoaded(), Mermaid сам бы отрендерил диаграмму. Ваш подход с MermaidBlock также рабочий; важнее, чтобы после внесения правок в код диаграммы Mermaid смог её распознать. Если диаграмма всё равно не отображается, проверьте консоль – возможно, нужно удалить атрибут data-processed у контейнера и вызвать повторную инициализацию (это бывает при повторных рендерах в React
stackoverflow.com
, хотя для статического финального вывода обычно не требуется).

Использование Confluence: Если вы собираетесь экспортировать итоговый документ в Confluence, учтите, что Confluence по умолчанию не рендерит mermaid-код самостоятельно. Необходим либо специальный плагин (Macro) для Mermaid, либо вставка уже готового изображения диаграммы. В Atlassian Marketplace есть приложение «Mermaid Diagram for Confluence» (зелёный значок), которое позволяет вставлять диаграммы через /mermaid макрос
community.developer.atlassian.com
. Если такой плагин установлен и активен, просто оставьте код диаграммы в содержимом страницы Confluence – плагин должен отобразить графику. Если плагина нет (или закончился триал, как у пользователя в форуме
community.developer.atlassian.com
), тогда Confluence будет показывать лишь сырой код. В этом случае правильным решением будет встраивать диаграмму как изображение. Вы можете программно сгенерировать SVG/PNG: либо с помощью Mermaid API/CLI на сервере до отправки в Confluence, либо сохранить SVG-код, полученный mermaid.render, в виде <img> тега в теле страницы. Таким образом, в Confluence сразу появится картинка схемы.

Следуя этим рекомендациям, вы добьётесь того, что диаграмма Mermaid корректно отобразится везде – в вашем приложении React она будет показана в виде схемы, а не текста, и при экспорте в Confluence вы увидите именно блок-схему. Главное – обеспечить правильное форматирование и исправить синтаксические нюансы Mermaid, тогда библиотека сможет преобразовать текст диаграммы в графическое представление 🌟.

Источники:

Инструкция по разметке диаграмм Mermaid в Markdown (код-блок с ```mermaid)
stackoverflow.com
.

Рекомендации по экранированию специального текста в узлах диаграммы (оборачивание текста в кавычки)
stackoverflow.com
.

Обсуждение интеграции Mermaid в Confluence (необходимость плагина для рендера на странице)
community.developer.atlassian.com
.